# Timeout in seconds before Limine auto-boots the default entry.
timeout: 0

/AnssOS
    # We use the native Limine boot protocol (not chainload/multiboot/etc).
    protocol: limine

    # boot():/ is the partition limine.conf itself was loaded from.
    path: boot():/boot/kernel
